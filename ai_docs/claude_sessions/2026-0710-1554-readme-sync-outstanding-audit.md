# Session: Outstanding-work audit → README sync (milestones 22–23) + bench gofmt

- **Session ID**: `ffb5895e-bfca-41f7-9299-52b8f25d8d28`
- **Date**: 2026-07-10 (session started 2026-07-09 evening)
- **Prior context**: post `1d24572` main (upsert / defaults / lastval arc complete, pushed). Asked to check the README and the last session docs for anything outstanding, then to fix what was found.
- **State at end**: `96e8e94` ("Update README for milestones 22-23; gofmt bench") committed on main, **not pushed**. Working tree clean otherwise. Both modules + bench build; `gofmt -l` clean repo-wide.

## The audit

Read `2026-0709-1347-upsert-defaults-lastval.md` and `2026-0709-1257-reconcile-parallel-returning.md`, cross-checked against the repo:

**Genuine loose ends found (both fixed this session):**
1. README stale — status said "Milestones 1–21" and the roadmap checklist ended at 21, while five feature commits had landed since (sequences/identity, RETURNING, ON CONFLICT, constant DEFAULTs, lastval/currval). Partial prior updates existed (ON CONFLICT and RETURNING were in the statement list) but SERIAL-in-CREATE-TABLE, column DEFAULT, DEFAULT VALUES, and lastval()/currval() appeared nowhere. The mkdocs site (`docs/features.md`, `docs/gotchas.md`) already covered everything — only the README lagged.
2. `bench/main.go` failed `gofmt -l` — pre-existing on both machines, noted in the reconcile doc.

**Deliberately deferred (unchanged, no demand):**
- Standalone `CREATE SEQUENCE` (stage 4 of the sequence arc) — now at least visible on the README's "Later" bullet.
- `pg_attrdef` still empty ⇒ psql `\d` shows a blank Default column even though defaults work.
- "Later" roadmap: order-aware path selection among redundant indexes, window frames, `LAG`/`LEAD`/`FIRST_VALUE`.

**Resolved / nothing to do:**
- `backup/local-returning-impl` no longer exists locally (may live only on the other machine).
- main was in sync with origin at audit time (fetched first — the cross-machine lesson from the reconcile session applied).

## The fix (`96e8e94`)

README edits, wording sourced from `docs/features.md` and the session docs rather than re-derived:
- **Status paragraph** → "Milestones 1–23", naming SERIAL identity columns, constant column DEFAULTs, RETURNING, and `INSERT ... ON CONFLICT`.
- **SQL frontend bullet** → INSERT annotated with upsert / identity / DEFAULTs.
- **Statement grammar** → `CREATE TABLE t (id serial PRIMARY KEY, c type [NOT NULL] [DEFAULT lit], ...)`; INSERT gains `| DEFAULT VALUES`.
- **New prose paragraph** (before the `FROM` paragraph) covering: serial = `GENERATED BY DEFAULT AS IDENTITY` sugar, durable per-column counter, explicit insert bumps the counter past itself (MySQL semantics — kills PG's duplicate-key-after-restore footgun), lastval()/currval() readback (with RETURNING as the one-round-trip way), the three DEFAULT application sites + `now()` rejection, and ON CONFLICT name resolution (bare names = existing row, `excluded.col` = proposed).
- **Roadmap**: new checked entries — Milestone 22 (identity columns + RETURNING) and Milestone 23 (write-path ORM enablement: upsert, DEFAULTs, lastval/currval); `CREATE SEQUENCE` appended to the "Later" bullet.

Plus `gofmt -w bench/main.go` (2-line diff).

## Gotchas / notes for next time

- **Push pending**: `96e8e94` (and this session-doc commit) are local-only — push before starting work on the other machine, or the reconcile scenario repeats.
- Milestone numbering precedent set here: 22 = identity + RETURNING arc, 23 = upsert/defaults/lastval arc. Future feature arcs continue from 24.
- When features land, the mkdocs site tends to get updated in-arc but the README does not — check README's status line + roadmap tail at the end of any feature arc.
- Still open if demand appears: `pg_attrdef` rows (psql `\d` Default column), standalone `CREATE SEQUENCE`.
